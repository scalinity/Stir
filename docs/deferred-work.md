# Deferred work — Stir

Tracked technical debt, deferred features, and triggered-when refactors. Extracted from `CLAUDE.md` 2026-05-01 to keep CLAUDE.md under its character budget without losing context. Same authority — when this file disagrees with code, follow the trigger rules below.

Each entry has an owner-step and a trigger condition. Don't fix proactively; wait for the trigger.

**Ticket coverage**: as of 2026-05-07, every active entry below has a corresponding Linear ticket under SCA-58 (SCA-89 through SCA-157). The ticket carries the full implementation plan; this file remains the trigger-condition tracker. When triggered, work the ticket; when shipped, move the entry here to "Recently resolved" with the SCA-NN reference.

---

## Active categories (high-level index)

- **Pre-launch chrome polish** — Cook Mode top-bar mockup divergence, Tonight `Other options` placeholder.
- **v1.1 / pre-public-launch backend** — Sentry tag deprecation rotation. (CCPA deletion flow shipped 2026-05-07 SCA-61; retention crons shipped 2026-05-07 SCA-63.)
- **Step 9 / ops hardening** — TanStack Query migration for ops SPA, ops SPA test harness, per-admin rate limit, HTTPErrorHandler refactor, SQL session_id rewrite, runaway_session detector, canned_fallback schema registry, APNs test mock, source-IP HMAC, voice-path mock transport.
- **Triggered-by-next-touch** — RealtimeSessionStateMachine sub-split (>1,800 LOC trigger), RealtimeSession proper-type extraction (post-SCA-79 follow-up), VoiceSessionDriver delegate collapse, VoiceSessionState path split, Clock injection.
- **Build/CI hygiene** — exhaustive-switch precommit check, post-commit build verification, CFBundleShortVersionString MARKETING_VERSION drift. (pre-push xcodebuild test hook shipped 2026-05-08 SCA-181; deno test side still TODO.)
- **Hard-pinned earlier deferrals** — IP rate limiting (step 3), Gemini Live API drift re-check (step 6 cheap-half), Mint endpoint auth re-test (step 6), CloudKit identity verification (step 6+), ActivityKit Live Activity (step 7).
- **Test-correctness debt** — voice_turn_usage_test ENT-VOICE-01→VOICE-SESSION-01 migration, pre-mint exact-boundary test, is_refresh=false direct pin, suspected flakes retained for re-recurrence diagnosis.
- **Documentation drift** — PostHogClient $ai_trace doc-comment, Spec §6 sync gap (METHOD-NOT-ALLOWED-01), error envelope drift protection.

---

## Items

- **Cook Mode tap-mode top bar diverges from the mockup — title lives in the top bar instead of a separate recipe strip** (SCA-93; Cook Mode title-truncation fix, 2026-04-28). `StepCardView.topBar` (`Stir/Features/CookMode/StepCardView.swift:112-141`) renders `[X | Spacer | recipeTitle | Spacer | 44pt-ghost]`. The 2026-04-28 fix added `.lineLimit(2) + .multilineTextAlignment(.center) + .minimumScaleFactor(0.85)` to stop "Rosemary Infused T…" truncation on long titles, but the underlying structure mismatches the mockup. `06_cook_mode_tap.html:70-85` specifies: top bar = `[X · 36pt rounded] [eyebrow "COOK MODE · TAP" + StepDots stacked] [eye-button · 36pt rounded]`, then a separate recipe strip below: `<recipe title (semibold)> · Step N of M  ···  ~T min left` (right-aligned). Refactoring requires (a) removing the title from `topBar`, (b) replacing X-button styling with the 36pt rounded background variant + adding a `StepDots` component for the eyebrow column, (c) adding a third right-side action (the eye/preview button — semantics TBD; potentially a recipe-detail peek), (d) inserting a `recipeStrip` view between `topBar` and the `ScrollView` showing title + step + time-left. Time-left is not currently computed anywhere — needs a sum-of-remaining-step-durations pass on `RecipePlan`. Trigger: pre-public-launch chrome polish OR when any other Cook Mode top-bar work happens, whichever first. Owner-step: as triggered.

- **`TonightHomeView.body` modifier-chain complexity is back at the SwiftUI typecheck ceiling** (SCA-94; 2026-05-05, post-Other-Options wire-up). SourceKit re-emits the "compiler is unable to type-check this expression in reasonable time" warning at `TonightHomeView.swift:81` after the fourth `.fullScreenCover` (Other Options) joined the stack. The actual swiftc compile still succeeds — the previously-applied mitigation (extract each cover's content into a `@ViewBuilder` method) is in place. But ANY further modifier addition to this body will likely tip over. Next touch must extract a sub-View (e.g. lift the cover-modifier cluster onto a `coverHost(coordinator:)` view modifier or a small private wrapper view). Trigger: any future modifier added to `TonightHomeView.body`. Owner-step: as triggered.

- **Pantry follow-ups from the multi-agent /review-orchestrator pass** (2026-05-05, post-pantry-management). The branch landed all 5 critical + 17 warnings + actionable suggestions inline; the items below are explicit deferrals captured in the same review.
  - **Pantry tombstone reaper** (SCA-97; SA3 W). `PantryItemRepository.softDelete` writes `deletedAt` and every UI/AI surface filters via `deletedAt == nil` predicates, so soft-deleted rows are invisible. CCPA scope is satisfied because the global account-delete path blows away the entire CloudKit zone. But there's no scheduled job, no `purgeTombstones(olderThan:)` API, no CloudKit subscription that hard-deletes `deletedAt != nil` rows after N days. Long-running users accumulate tombstones indefinitely, growing CloudKit zone size and eventual restore-from-iCloud time. Trigger: alongside the v1.1 retention-cron work for `ai_request_log` etc. Owner-step: v1.1.
  - **`PersistenceController.save():86` logs raw NSError at `.public`** (SCA-98; SA3 W). `Logger.coreData.error("viewContext save failed: \(error.localizedDescription, privacy: .public)")` logs the raw `NSError` from `context.save()` BEFORE wrapping in `StirError.coreData(underlying:)`. Core Data's `NSValidationErrorKey` userInfo path produces strings like `"<displayName>" is not a valid value for the attribute "displayName"` — embeds the user-supplied value verbatim. Pantry feature is the first to broadly route user-typed strings into Core Data validation, making this latent leak exploitable in practice. Pantry-surface VM logger callsites already use `.private` defensively (commit 39617b3), but `PersistenceController` is upstream of every repo. Fix: change line 86 to `privacy: .private(mask: .hash)` or strip to error code + domain only. Trigger: any change to `PersistenceController.swift`, OR pre-public-launch privacy audit. Owner-step: as triggered.
  - **Tier-downgrade pantry reconciliation policy** (SCA-99; SA2 M). User trials Premium, scans/adds 200 `.remembered` rows (cap 250 ✓), trial expires → tier flips to `.free` (cap 25). The 200 existing rows stay; new adds reject with `.capReached`; header reads "200 of 25 saved" — visually broken. ADR-0028 didn't pick a position. Two viable fixes: (a) on tier-downgrade hydrate, soft-archive the oldest `(used - cap)` `.remembered` rows to `.ephemeral` (preserves data, restores cap math); (b) clamp header display at cap and document Free-grandfathering as intentional. Either path needs an ADR. Trigger: first user reports the "200 of 25" header anomaly OR pre-public-launch chrome polish. Owner-step: as triggered.
  - **Cap source-of-truth migration to server** (SCA-100; CR1 W). Today the cap value table lives on `Tier.rememberedPantryCap` (centralized in commit 39617b3 from 4 hardcoded sites). The next refinement is shipping the cap as a non-metered entitlement on `/v1/config/bootstrap` so the value is server-resolvable per A/B test, marketing override, etc. Pantry surface would read from `entitlements.standing_pantry_cap` instead. Trigger: marketing wants to A/B-test the Free cap, OR cap value moves more than once. Owner-step: v1.1.
  - **Pantry performance — higher-effort items** (SCA-101 follow-up; cheap wins (a)/(c)/(d) shipped 2026-05-08). Item (b) "surgical mutation of items on add/edit/delete instead of full load() re-fetch" was deferred from SCA-101 because @Observable doesn't track NSManagedObject property changes — surgical mutation requires either reassigning the array (`items = items` is a no-op under Observable identity tracking; need an explicit re-bind to a fresh `Array(items)`) or restructuring `rememberedCount` to live off a count cache rather than `items.filter`. Higher-effort items still on the table: (i) item (b) above; (ii) pre-lowercased name index for filter (avoids the per-row `.lowercased()` allocation under typeahead); (iii) batched `fetchExisting` for `upsertFromScan` (collapse N queries to 1); (iv) Core Data composite index `(household, deletedAt, lastSeenAt)`. Trigger: Pro-tier user reports lag persists post-SCA-101, OR Instruments captures show `filteredItems` body-eval allocations >5% of frame time. Owner-step: as triggered.

- **SCA-56 leftovers-handoff follow-ups from the multi-agent /review-5 pass** (2026-05-06, post-SCA-55-fixes). Rolled-up review items addressed inline this commit captured ~95% of findings; the items below are explicit deferrals.
  - **CookModeRoot LOC tracker** (SCA-104; CR1 S9 / S17). After SCA-55 wiring CookModeRoot is at ~830 LOC (+177 from this PR + ~50 SCA-56). Below the 1915-LOC trigger CLAUDE.md cites for CookModeViewModel telemetry extraction, but the leftovers subsystem (intent dispatch, sticky-intent observer, presentLeftovers, handleLeftoverDishSelected, helper methods, two `@State` Task handles + cancellation) is now a coherent self-contained block that extracts cleanly into a `CookModeLeftoversCoordinator` or similar. Trigger: next CookModeRoot touch that adds another concern, OR the CookModeViewModel >1915 LOC extraction cutover. Owner-step: as triggered.
  - ~~**`controller.save()` UI-freeze risk on pathologically large leftovers dishes**~~ — **resolved 2026-05-08 by SCA-106**. `createLeftoversSolveWithDish` is now `async` and runs the full entity construction + `save()` on a private-queue NSManagedObjectContext spun up via the new `PersistenceController.newBackgroundContext()` helper. Returns `LeftoversSolveOutcome { recipePlan, persistMs }` — the RecipePlan is re-fetched on viewContext via `existingObject(with:)` so the @MainActor caller gets a main-bound managed object, and `persistMs` is wired through `leftovers_dish_selected.persist_ms` for the long-tail dashboard signal that motivated the migration (spec §15 + CLAUDE.md telemetry list updated in the same commit). 50-ingredient + 25-step pathological fixture pinned in `SolveRepositoryLeftoversTests.test_createLeftoversSolveWithDish_pathologicalDishPersistsViaBackgroundContext`.
  - **`latestTonightPick` auto-promotion of leftovers plan may surprise users running Leftovers as a side experiment** (SCA-108; CA1 #6 / CR1 cross-cutting). The new RecipePlan unconditionally becomes Tonight's hero card via `latestCompletedSolve` ordering; the only escape is running another regular solve. Spec D4/D5 ratified this as the "user lands somewhere logical" mechanism. If complaints surface, options are (a) UI cancel-without-persist on LeftoversRoot, (b) `latestTonightPick` filter `AND sourceRecipePlanId IS NULL`. Trigger: user reports "I tried leftovers and now Tonight is wrong." Owner-step: as triggered.
  - ~~**`MealSolveRequest.sourceRecipePlanId` orphan acceptance**~~ — **resolved 2026-05-08 by SCA-110**. `sourceRecipePlanId` (bare UUID attribute) was promoted to `sourceRecipePlan` (Core Data to-one relationship → `RecipePlan?`, `deletionRule=Nullify`) with inverse `RecipePlan.leftoversSolves` (to-many, `deletionRule=Nullify`). Lightweight migration of the existing `Stir.xcdatamodel` (in-place edit; no model-version bump because the codebase has no prior versioning precedent and `codeGenerationType="class"` on a sibling `Stir 2.xcdatamodel` produced a managed-object-class-uniqueness conflict at runtime — see commit message for the empirical write-up). Pre-existing dev-DB rows lose their old UUID column but the new relationship populates correctly on first write; old data isn't carried forward via the in-place edit, which is acceptable pre-ship. `solve.sourceRecipePlanId` consumers in code (1 read site in `latestTonightPick`, 1 write site in `createLeftoversSolveWithDish`) migrated to the new relationship. New unit test `test_createLeftoversSolveWithDish_sourceRecipePlanDeleteNullsRelationship` pins Nullify semantics — deleting the source plan leaves the leftovers solve row intact with `sourceRecipePlan == nil`, no cascade. Full StirTests suite: 514/514 green.

- **CFBundleShortVersionString hard-coded `1.0.0` in 3 Info.plist files will drift from `MARKETING_VERSION` on next bump** (SCA-92; step-9 review CA2-L3 / DB1-B5). `Stir/App/Info.plist:19-20`, `Extensions/StirShareExtension/Info.plist:19-20`, `Extensions/StirWidgets/Info.plist:19-20` all hard-code `1.0.0` / `1`. `pbxproj` declares `MARKETING_VERSION = 1.0.0` / `CURRENT_PROJECT_VERSION = 1` in 4 build configs. Today they match; on next bump (e.g. `MARKETING_VERSION = 1.0.1`), pbxproj updates but plists don't — TestFlight rejects the upload as "duplicate build" or warns silently in CI. Fix: replace plist literals with `$(MARKETING_VERSION)` / `$(CURRENT_PROJECT_VERSION)` AND set `INFOPLIST_PREPROCESS = YES` across all 4 build configs (currently `GENERATE_INFOPLIST_FILE = NO` for extensions, `YES` for main app Release; preprocessor needs to be on for `$(VAR)` substitution to fire). Earlier step-9 attempt at this without flipping `INFOPLIST_PREPROCESS` left the literal `$(MARKETING_VERSION)` string in CFBundleVersion (build warned "parent app (null)") — verified now-reverted. Trigger: at next `MARKETING_VERSION` bump (any version > 1.0.0). Owner-step: as triggered (likely v1.0.1 hotfix or v1.1).

- **Deletion-request fulfillment worker — pgmq consumer + cross-system erase** (SCA-88, opened 2026-05-07). SCA-61 shipped the in-app surface + endpoint + table + ops admin tab. What's still manual at the moment of approval: a `pending → approved` transition lands a row but no downstream worker walks the `external_refs_json` checklist (PostHog identify-merge → Sentry erase → RevenueCat alias cleanup → CloudKit zone-delete trigger → Postgres row sweep). Owner-step: pre-public-launch (Privacy Policy §7.2 promises 30-day fulfillment SLA, currently owner-bottlenecked).

- ~~**TanStack Query migration for ops SPA**~~ — **resolved 2026-05-07 by SCA-82** (CostAnomaliesPage, FlaggedOutputsPage, UsersPage, DashboardPage all migrated to useQuery + useMutation; useEffect-disable comments removed). Original entry retained in "Recently resolved" below. Step-8 kickoff brief deprioritized SPA React-discipline to keep the ops console minimal, but the landed code knowingly violates the global `~/.claude/CLAUDE.md` "Never use `useEffect` directly" rule across `DashboardPage` / `UsersPage` / `FlaggedOutputsPage` / `CostAnomaliesPage` with `/* eslint-disable-next-line */` escape hatches at each fetch site. Not a future enhancement — a scoped policy exception with a named migration path. Resolve by migrating to TanStack Query (retry + stale-while-revalidate + request deduplication come for free). Trigger: when the ops SPA grows beyond the 4 current pages, OR when step 9 polish lands, whichever comes first. Owner-step: step 9.

- **ops SPA runtime test harness** (SCA-113; step-8 review 4a/4b observation). Vitest scaffolding shipped in SCA-82; Playwright e2e + DeletionRequestsPage smoke remain. No Vitest, no React Testing Library, no Playwright in `ops/`. Current coverage: TypeScript strict-mode compile (`tsc -b`) + Vite bundle (`npm run build`). Regressions in user-facing flows (dialog-confirm-then-API-call, page transitions, error states, loading states) are caught at runtime only via manual smoke testing before beta. Minimum viable v1: Vitest + React Testing Library + one smoke test per page that renders without error + one end-to-end test for the most-used flow (typically `users.list` → detail → `force_reauth`). Triggers to reconsider: (a) ops SPA grows beyond 6 pages, (b) user-reported regression traces to uncaught-by-build logic, (c) multi-page refactor would benefit from regression tests. Owner-step: step 9.

- **W16 — `processPushSend` integration coverage** (SCA-115; step-8 review). Scaffold (`pgmq_dispatch_push_send_test.ts`) was reverted because the test gate `STIR_RUN_APNS_INTEGRATION=1 && STIR_APNS_MOCK_URL` silently skipped every assertion, producing a trap-shaped file that LOOKED like coverage while exercising nothing. Real coverage requires: (a) a Deno-based mock APNs HTTP/2 server, (b) an `APNS_HOST_OVERRIDE` env path in `apns.ts` OR an equivalent reachability seam — **may be cleaner to inject an HTTP client rather than override host** (the host-override approach was rejected here because it's production config that exists only for one test; dependency-injection at the fetch seam is the less-invasive alternative), (c) test setup that starts the mock server, points the dispatcher at it, and exercises 200 / 410 / 503 branches. Trigger: when step-9 APNs compliance-test infrastructure lands — that workstream will need the same mock server + seam; W16 folds into it naturally instead of being an orphan refactor. Owner-step: step 9.

- **Per-admin rate-limit bucket** (SCA-117; step-8 review W6 follow-up). The global `ip:ops_admin_hourly` cap (30/min = 1800/hour per IP) landed in `rate_limiter.ts` (commit 1d). A per-admin bucket keyed on `admin.authUserId` would tighten the defense against a single-admin-account compromise that rotates IPs. Ship when admin count > 5 OR ops_admin call volume makes the global cap noisy. Owner-step: step 9.

- **SupabaseSessionClient HTTPErrorHandler.map refactor** (SCA-119; step-8 review CR2 W6). `perform` / `performNoContent` / `performStream` each carry a ~100-LOC error-mapping block (401/403/429/5xx/VOICE-SESSION-01/VAL-01) with the `reauthRequired` branch duplicated across all three (`SupabaseSessionClient.swift` lines 197, 389, 484). Extract a shared `HTTPErrorHandler.map(status, data, request)` helper; the three perform-variants become thin shells around a shared status-translation function. Trigger: any new AUTH-01 reason or a 4th perform variant. Owner-step: step 9.

- **`stir_ops_cost_anomaly_scan` session_id rewrite + runaway_session detector emit** (SCA-121; step-8 review W33, post-bundle). Migration 20260424000003 added `ai_request_log.session_id UUID` + partial index; `voice-turn-usage` populates it on new rows and the migration backfilled existing voice rows. But the scan function's voice branch still uses `request_id LIKE 'voice:%:%'` + `split_part(request_id, ':', 2)` rather than `session_id` directly. Also: the `runaway_session` anomaly ENUM value is declared (migration 20260423000008) but no detector emits it. Rewrite `stir_ops_cost_anomaly_scan` to use `session_id` (fast, index-backed) and emit `runaway_session` for sessions with >10-min span AND >20 turns. Trigger: 7+ days after backfill stabilizes (so all voice rows older than the analysis window have `session_id` populated). Owner-step: step 9 before beta scale ramp.

- ~~**Per-feature `canned_fallback_json` schema registry**~~ — **resolved 2026-05-07 by SCA-81**. Per-feature top-level-key allowlist validator shipped at `Backend/supabase/functions/_shared/canned_fallback_schemas.ts`, wired into `ops-admin/handleFlaggedOutputsResolve`, 10/10 unit tests green. Original entry in "Recently resolved". Current Zod accepts any JSON object within a 64 KiB size cap (`ops-admin/index.ts` `FlaggedOutputsResolveParams`). Full safety requires validating against a per-feature response schema keyed off the flagged row's `feature_key` (`cook_mode_realtime` vs `substitution` vs `recipe_import` etc.) before the cache UPDATE. Discriminated-union registry. Trigger: step 9 before beta opens the ops console to non-Daniel admins (pre-beta canned-fallback payloads need structural correctness guarantee to prevent iOS decode failures on the next cache hit). Owner-step: step 9.

- **APNs untested changes — W10 / W11 / W44** (SCA-123; step-8 review commit 2 carry-forward). Commit 2 added three hardening changes to `_shared/apns.ts` that lack direct test coverage: **W10 timeout** (`AbortSignal.timeout(10_000)` — requires slow-TCP simulation infra to test the abort path); **W11 mint coalescing** (`mintingPromise` serializing concurrent mints — requires concurrent-request test harness); **W44 PKCS#8 parse error wrap** (improved error message on malformed p8 — deploy-time-only path, not request-time). These land as defense-in-depth behind existing fetch-mocking tests. Trigger: when step-9 APNs compliance-test infrastructure lands — shares the mock APNs HTTP/2 server work with W16, all three changes become testable. Owner-step: step 9.

- **pgmq-dispatch reclaim sweep as SQL stored proc** (SCA-125; step-8 review commit 3b carry-forward). The two-part reclaim sweep (Part A reclaim to pending, Part B dead-letter to failed at attempt_count=MAX) is implemented in TypeScript within `pgmq-dispatch/index.ts`. This makes sweep tests edge-runtime-dependent (tests POST to `/functions/v1/pgmq-dispatch` to invoke the sweep), which complicates isolated-worktree verification under the Path A hybrid protocol. Alternative: extract into a SQL stored proc `stir_pgmq_reclaim_sweep()` callable via `svc.rpc(...)`; the TypeScript handler invokes the proc. Tests become direct-DB, which is cleaner isolation. Current TypeScript implementation is functionally correct; the refactor is test-harness ergonomics. Trigger: next sweep-logic change, OR when a multi-commit bundle work re-introduces the Path A contamination pain for pgmq tests. Owner-step: step 9.

- **Zod/CHECK synchronization invariant** (SCA-127; step-8 review 1c follow-up). Size caps on `ops_flagged_outputs.context_snapshot_json`, `canned_fallback_json`, and `flag_reason` are enforced at two layers: Zod `.refine()` in edge handlers (`ops-flag-output/index.ts`, `ops-admin/index.ts`) + SQL CHECK constraints in migration 000005. Defense-in-depth is intentional; the cost is synchronization discipline — a future tighten or loosen of any cap requires updating BOTH layers together. A handler-side loosen without a DB loosen produces 500s where the API accepted a payload the DB rejects; a DB-side tighten without a handler tighten produces invisible tech debt. Trigger: any change to `CONTEXT_SNAPSHOT_MAX_BYTES`, `CANNED_FALLBACK_MAX_BYTES`, or the `flag_reason` 500-char cap; OR any migration adjusting the matching CHECK constraints. A test that asserts cap values match between layers would pin this; absent one, manual review discipline. Owner-step: as triggered.

- **Isolated-worktree verification limitation — shared Supabase edge runtime** (SCA-128; step-8 review 1b verify observation). `supabase start`'s edge-runtime Docker container mounts from the directory where the stack was started (main tree), not from the verify worktree. Tests POSTing to edge functions hit the mount-point handler code, not the verify-SHA handler code. Workarounds: (a) **Path A hybrid** — run only tests whose target handlers haven't diverged between main and verify, document skips; (b) **Path B full** — `supabase stop && cd <worktree> && supabase start` per verify, 60-90s ceremony cost per commit. Path A is default; Path B is explicit override for correctness-critical verifies. Ideal fix: isolated Supabase stack per worktree (docker networking work, not currently trivial). Trigger: when a multi-commit bundle has ≥3 commits and the Path A skip list exceeds 30% of the relevant tests. Owner-step: as triggered.

- **`RealtimeSessionStateMachine.swift` sub-split** (SCA-79 review W5 / SCA-163; multi-agent /review-orchestrator pass CR1, 2026-05-08). Post-SCA-79, the StateMachine bucket is ~1,420 LOC and contains two natural sub-concerns: (a) refresh + pre-mint + watchdog session-management (~425 LOC) and (b) per-turn frame routing + finalize + persist + transport-error recovery (~990 LOC). The `LiveSessionRefreshCoordinator` from the original SCA-79 charter is sitting right there in the first half. Future extraction shape: `RealtimeSessionRefresh.swift` (refresh + pre-mint + watchdog + recap + sanitizer); `RealtimeSessionStateMachine.swift` keeps frame routing + turn lifecycle + finalize/flush + persist + handleTransportError + recordTurnAsTransportError. Trigger: file crosses ~1,800 LOC, OR on next major touch (a "substantive" change in the SCA-79 sense — new method, lifecycle change, multi-line behavior change). Owner-step: triggered.

- **`RealtimeSession` proper-type extraction (post-SCA-79 follow-up)** (SCA-79 review W9 / SCA-163; multi-agent /review-orchestrator pass CR1, 2026-05-08). The SCA-79 split delivered a 4-file Swift-extension reorganization but did NOT deliver the original charter's class extraction (RealtimeTransport / RealtimeAudioPipeline / RealtimeSessionOrchestrator + LiveSessionRefreshCoordinator). Stored properties remain on the single `@MainActor final class RealtimeSession` host type; cross-bucket method calls use `self`; extension visibility forced 30+ properties from `private` → internal (mitigated in SCA-159 via comment guards). The deferral is legitimate scope-management for an in-flight Premium feature on a preview API — but it is load-bearing tech debt, not invisible. **Triggers** (any one): (a) any per-bucket file (Transport, StateMachine, AudioIO) crosses ~1,800 LOC; OR (b) Gemini Live moves out of preview / to GA (preview-API churn no longer dominates risk); OR (c) a third bucket needs cross-coupling that today's extension-on-final-class model can't express cleanly (e.g. a real `LiveWebSocketTransportProtocol` seam usable from production rather than test-only); OR (d) a fourth race-sensitive flag joins the existing `finalizeWasWatchdogFire` / `finalizeWasTransportError` / `isRefreshing` triplet (the property explosion is itself the smell). Production transport seam linkage: the proper extraction is the natural place to introduce a real `LiveWebSocketTransportProtocol` (today the test harness uses an internal test-only protocol — see `LiveWebSocketTransport.swift`). The proper-type version's `LiveSessionRefreshCoordinator` should own the seam. Owner-step: triggered.

- **`CookModeViewModel.swift` telemetry extraction — LOC trigger tripped a second time** (SCA-80; multi-agent code review CR1-S1 / CR2-W3, 2026-05-04). 2,333 LOC vs prior 1,915 trigger; 2026-05-04 batch added 358 more lines (transcript display, voice-UI flag, error-result-type routing). Each new field reaches into the same monolithic VM. Extract `VoiceSessionPresenter` (driver + rebuild + trace state, including the 7 sites that flip `isVoiceUIRequested`) and `CookModeTelemetry` (LiveTurnSummary accumulator, voice-session-trace, cook-turn emission). Then VM is back to step-advance + substitution + timer surface area. Trigger: next Cook Mode feature work, OR after step-6 polish. Owner-step: step 6 polish.

- **Settings DesignSystem primitive duplication — `sectionEyebrow` + `rowDivider` + principal-toolbar repeated 3-4×** (SCA-95; multi-agent code review CR2-W1 / FD1-4, 2026-05-04). Files: `Stir/Features/Settings/{SettingsRootView,HouseholdPreferencesView,NotificationPrefsView}.swift` + `Stir/Features/CookMode/OutcomeFeedbackView.swift`. Three copies of `sectionEyebrow` are byte-identical; `rowDivider` differs only on a leading inset (Settings inserts `iconTileSize`, others use `space3Half`). The principal-toolbar `ToolbarItem + .toolbarBackground` block is duplicated across 4 Settings/preferences screens. Saved bypasses with `.safeAreaInset(.top)` + `.toolbar(.hidden)` so the pattern isn't universal. Lift `SectionEyebrow(_ text:)`, `StirRowDivider(insetMatchingTile: Bool)`, and `.stirNavigationTitle(_ title:)` view modifier into `Stir/DesignSystem/Components/`. Cost: ~150 LOC removed, 3 new components added; risk is style regression on the now-shared abstractions, so the change should land alongside a pixel-comparison pass (compare each Settings screen to its mockup before/after). Trigger: next Settings or onboarding screen change, OR pre-public-launch chrome polish. Owner-step: step 9 chrome polish.

- **`PlateIllustration` duplicated between `TonightHomeView` and `DishOptionCard.PlateView`** (SCA-96; multi-agent code review CR2-W4, 2026-05-04). 138 LOC of decorative SwiftUI shapes (rice grains, sesame seeds, fillet-grain Path) live next to Tonight's logic; the same plate concept rebuilt as `DishOptionCard.PlateView`. Two private duplicates, one job. Unify into `Stir/DesignSystem/Components/StirPlate.swift` parameterized on `(size: tint:)`; drop both privates. The TonightHomeView variant has a salmon-fillet GeometryReader still present (FD1-11) — refactor moment is the time to drop it. Trigger: when the next view wants a plate illustration, OR pre-public-launch chrome polish. Owner-step: step 9.

- **Tonight `cookingSessionRepository`-via-coordinator wiring complete; tests now stub-able** (multi-agent code review CR1-W4 fix, 2026-05-04 — recorded for future testers). `TonightHomeView.refreshState` now reads `coordinator.cookingSessionRepository` instead of constructing a fresh repo per refresh. Adds the test seam to stub `resumableSession(for:)` from `RootCoordinator` initializers. No further work needed; entry exists so the next test file looking for a repo seam knows it lives at `coordinator.cookingSessionRepository`. Owner-step: complete (informational).

- ~~**`CookModeViewModel.matchIngredient` longest-match preference**~~ — **partially resolved 2026-05-08 by SCA-148 (Phase 1: free-text fallback + telemetry)**. `matchIngredient` now returns a typed `IngredientMatchResult` enum distinguishing `.exact`, `.substring(row, candidateCount)`, `.tie([rows])`, `.none`. The voice path's `applyVoiceSubstitution` consumer routes the tie case to the free-text persistence path (no recipe row mutation; SubstitutionEvent persisted with no FK) instead of the prior arbitrary-winner pick. New telemetry event `voice_substitution_disambiguated` (spec §15 + CLAUDE.md telemetry list landed in same commit) carries `{session_id, candidate_count, surface ∈ {voice, tap}, resolved_to ∈ {free_text_fallback, user_picked_first, user_picked_second, timed_out}}`. v1 emits ONLY `free_text_fallback`; the other three resolution values are reserved for the future "ask the user mid-turn" build-out (Phase 2 — needs RealtimeSession state-machine work + 5s response window). Phase 2 is gated on the original ticket trigger: "≥1% of voice substitutions hitting the same-length-tie path" — measured via the new event. Until then, the tie-case ambiguity is captured (no more wrong-row mutations) and observable.

- **`pgmq-dispatch` shared-secret rotation runbook** (SCA-149 + SCA-157 Vault migration; multi-agent code review SA2-Medium fix, 2026-05-04). Migration `20260504000001_pgmq_dispatch_shared_secret.sql` added the `STIR_PGMQ_DISPATCH_SECRET` gate — function side reads the env var, Postgres side reads `app.stir_pgmq_dispatch_secret`. The migration has the production deploy procedure inline. Add a runbook entry at `docs/runbooks/pgmq-dispatch-secret-rotation.md` covering rotation cadence (quarterly), the brief AUTH-01 + queue-lag window during deploy, and the `supabase secrets set` + `ALTER DATABASE ... SET app.stir_pgmq_dispatch_secret` two-sided rotation steps. Until the runbook exists, the production-deploy-procedure comment in the migration is canonical. Trigger: first beta invite OR before the function URL is exposed publicly. Owner-step: pre-beta hardening.

- ~~**`hard_rules.ts` allergen FP follow-up — narrow allowlist for botanically-safe foods**~~ — **resolved 2026-05-08 by SCA-150 (Option A)**. `_shared/hard_rules.ts` exports `ALLERGEN_BOTANICAL_SAFE_NOTES` mapping `nut` / `tree_nut` / `peanut` to a shared narrative clause naming `coconut`, `butternut squash`, `nutmeg` as botanically-safe and explicitly keeping `pine nut` on the avoid-list (cross-reactivity). `dinner-solve/index.ts` ships a new exported `buildReplacementUserText(rank, violations)` helper that appends the clause once per call when violations include any allergen kind whose value is in the table; `requestReplacementDish` consumes the helper. Validator semantics are unchanged — the clause only nudges the regenerator's SECOND-pass replacement reasoning, never the first-pass match. ADR 0032 documents the decision plus why we did NOT bump `prompt_versions.version` (template_blob is unchanged; the change is in code). Pure unit tests (8/8) in `tests/dinner_solve_replacement_user_text_test.ts` pin shape + dedupe.

- ~~**IP-based rate limiting on `/v1/session/bootstrap`**~~ — **resolved in step 3**. `ip:bootstrap_hourly` policy live in `_shared/rate_limiter.ts` (20/hr per IP); hits the bootstrap handler. Original entry retained for context only. Rationale: bootstrap rate limiting alone protects nothing until `/v1/ai/*` endpoints exist. Lands with `/v1/ai/dinner-solve` and shares infrastructure with `ai_request_log` for cost observability. Implementation sketch: Postgres `rate_limit_buckets` table, sliding window, 30 solves/day per IP across canonical keys. Supabase platform-layer rate limiting is the backstop in the meantime.

- **Gemini Live API drift re-check** (SCA-134; cheap-half spike): scheduled for start of step 6. Purpose is to catch API drift between the April 2026 full spike (already complete) and step-6 kickoff. Step 6 cannot start without it.

- **Mint endpoint auth unknown from April 2026 spike** (SCA-135): `POST /v1alpha/authTokens` returned `400 INVALID_ARGUMENT` on API-key auth in the spike environment. Re-test from the actual Supabase Edge Function at step 6. Fallbacks if it still fails: OAuth service-account auth, or backend-proxied WebSocket (both keep the Gemini key server-side).

- **Server-side CloudKit identity verification** (SCA-136; known trust boundary): `cloudkit_user_record_name` in `/v1/session/bootstrap` is currently taken on trust from the iOS client. A modified client can claim any canonical_user_key by submitting another user's CK record name. Step-1 mitigation: Zod regex `^_[a-f0-9]{32}$` rejects malformed names at the boundary. Full mitigation (deferred to step 6+): server-to-server verification via CloudKit Web Services, or a short-lived CK-signed challenge token iOS must include in bootstrap. Risk acceptable at v1 because CK record names are opaque 32-hex strings not easily enumerable, and the attacker gains only this-user-scoped operational data (user content is CloudKit-private-DB-scoped and unreachable from Supabase).

- **ActivityKit Live Activity for Cook Mode timers** (SCA-137; deferred from step 4 to step 7). Step 4 ships `TimerService` with in-app `TimelineView` countdown + `UNUserNotification` scheduling on fire date, which covers the critical-path UX. Live Activity on Lock Screen + Dynamic Island requires a Widget Extension target (new bundle ID, `NSSupportsLiveActivities` entitlement, Widget Extension plumbing). Step 7 adds the Widget Extension for Home Screen widgets; Live Activity piggybacks on that same target. Correlation between already-scheduled `UNNotificationRequest`s and Live Activity content is handled via `CookingSession.localNotificationIdsJSON` (already persisted in step 4).

- ~~**Persisted pause timestamp for `CookTimer`**~~ — **decided 2026-04 / not adding**. State machine + in-memory pause tracking covers active-session pause/resume; cross-session paused timers lose pause context and resume with zero paused-duration (TimerService.swift flags this explicitly). No ticket — revisit only if user reports of "paused timer shows wrong time" cross a threshold in beta. State machine + in-memory pause tracking covers active-session pause/resume; cross-session paused timers lose pause context and resume with zero paused-duration (TimerService.swift flags this explicitly). Revisit only if user reports of "paused timer shows wrong time" cross a threshold in beta.

- **`VoiceSessionDriver` delegate collapse** (SCA-130; step-6 review CR1-W3). Collapse the five+ individual `on{Event}` closures on `RealtimeSession` into a `VoiceSessionDriverDelegate` struct of optional closures set on the protocol; `CookModeRoot.wireVoiceDriver` then doesn't need runtime `as? RealtimeSession` downcasts and the compiler catches missing callback wiring when new events are added. Trigger: next time a new Live-only callback is added to `RealtimeSession`. Owner-step: as triggered.

- **`VoiceSessionState` path split** (SCA-131; step-6 review CR1-W4). Current `canTransition` grammar has been relaxed enough that it rejects only a handful of edges; real value lives in the phase-boundary checks, not individual edges. Split into `LivePathState` (connecting / toolCalling / refreshing) and `FallbackPathState` (transcribing), each with a tight grammar; the VM exposes a coarser union for UI. Trigger: next time a new state gets added to cover a preview-API quirk OR when the `canTransition` body exceeds 80 lines. Owner-step: step 9.

- **Source-IP HMAC with rotated salt** (SCA-129; step-6 review P2-C). Current `ipBucket()` (`_shared/rate_limiter.ts`) uses unsalted FNV-1a — deterministic, reversible in seconds against the IPv4 space. Suitable for dashboard deduplication, NOT for privacy. Replace with HMAC-SHA256 keyed on an env-sourced `LOG_IP_SALT` (32 random bytes stored in Supabase secrets), rotated monthly via a scheduled task that mints a new salt, retains the prior for a 24h overlap window, then discards. Fallback: if `LOG_IP_SALT` is missing, log `source_ip_bucket: "unsalted"` so misconfig is observable. Trigger: before step 9 beta OR when log-retention policy tightens to require privacy-grade hashing. Owner-step: step 8 (ops hardening) or step 9.

- **Voice-path architectural test harness** (SCA-132; step-6 review P1-S extension). `RealtimeSessionRecoveryTests` covers the `.skipped` path of `RefreshOutcome` via direct calls; `.success` / `.preCommitFailure` / `.postCommitFailure` require a mock WebSocket transport + scriptable mint response. Build once as shared `MockLiveTransport` + `MockMintResponse` factories; unlocks P1-S full coverage and future refactor-driven tests. (P1-P was completed via direct task injection per its own harness design — didn't need MockLiveTransport.) Owner-step: step 8.

- **Pre-commit check — exhaustive-switch verification.** When a commit adds a new enum case to a type consumed by exhaustive `switch` statements, verify all consumers updated in the same commit. Trigger: any diff touching `enum` in a file under `Core/` or `Shared/`. Check: `grep -rn "switch.*\(EnumName\)" --include="*.swift"` — every hit must be in the same commit or the build breaks. Self-observed: commit 251401b landed `ErrorCode.voiceSession01` without the `ErrorPresenter` arm; fixup required. Owner-step: any agent, any commit.

- **Post-commit build verification on main.** No commit lands on main without a green `xcodebuild build` in an isolated worktree. Applies to all agents. Red-main window observed 2026-04-23: step-8 agent's source commits `ed07a26..9f8d131` (inclusive) landed without pbxproj sync, breaking main build for multiple commits in a row; caught only by the step-6 review sweep at commit 79c8701. Same sweep caught exhaustive-switch omission in 251401b. Owner-step: any agent, any commit.

- **Pre-push hook for green-build enforcement.** Add a git pre-push hook (or CI equivalent) running `xcodebuild build -scheme Stir -destination 'platform=iOS Simulator,name=iPhone 17 Pro'` AND `deno test Backend/supabase/tests/` before allowing push to main. No such hook exists today. Trigger: next time an agent lands a red commit on main. Owner-step: ops hardening (step 8 or later).

- **Error envelope drift protection.** Server error response shape (`{ error, message, reason }` top-level) is specified in Spec §6 paragraph text but not enforced by code. Future refactors to the envelope shape will silently drift from spec. Consider a shared TypeScript type re-exported from `_shared/errors.ts` and a test that asserts the wire shape matches the spec example. Trigger: next time the envelope shape changes, OR before step 9 beta (whichever comes first). Owner-step: step 8 (ops hardening) or as triggered.

- **Clock injection across voice-path time-dependent code** (SCA-133). `SpeechFallbackService.speak` uses a property-injected timeout + `_testSetSpeakTimeoutSec` hook (P1-Q, `05787c3`). `RealtimeSession` uses `Date()` for pre-mint staleness + turnStuckWatchdog age + pre-warm timing — each test-instrumented via private-state test hooks. `refreshSession` uses `Task.sleep` for backoff. A shared `any Clock` injection pattern would unify these, enable exact-boundary time-sensitive tests, and remove the ad-hoc hooks. Trigger: when the third such test-hook is added, OR when a time-dependent bug requires property-based testing. Owner-step: step 8 (ops hardening) or as triggered.

- **Pre-mint staleness exact-boundary test** (SCA-144; blocked by SCA-133). P1-P tests 4a + 4b (commit `06cde46`) pin both sides of the 45 s staleness boundary with a 1 s margin. They do NOT distinguish `<` vs `<=` at age == `preMintStalenessSec` exactly. Pinning requires time-injection on `consumePreMintedTaskIfFresh`. Trigger: next touch to the pre-mint consumer, OR when `LiveSessionBudget.preMintStalenessSec` changes and the boundary semantic needs re-pinning. Owner-step: as triggered (Clock-injection §Deferred entry unblocks this work).

- **`is_refresh=false` quota-increment direct pin** (SCA-145). P1-N test 1 (commit `aa51e7f`) verifies net `used_count` invariance on `is_refresh=true` (implicitly pinning both refund guards). The `is_refresh=false` increment-then-refund path on mint failure can't be distinguished from never-incremented on the placeholder-key CI path — post-call state is identical. Requires either integration tests against a real mint (`STIR_RUN_AI_INTEGRATION_TESTS`) OR refund-audit observability to disambiguate. Trigger: when real-mint integration tests are enabled, OR when refund observability lands. Owner-step: as triggered.

- **`voice_turn_usage_test.ts` owner-binding tier-2 migration** (SCA-146). `no-owner-row` and `IDOR` tests assert on `ENT-VOICE-01` and pass via entitlement-gate-short-circuit rather than exercising the `VOICE-SESSION-01` owner-binding path per ADR 0017. Not active-red (the session_closed test was migrated in `fd32f29` and the audit commit `e117af4` landed with 328/328 green), but these two tests pin the wrong behavior. Restore correctness by promoting to Premium + migrating assertions to `VOICE-SESSION-01` with `reason` discriminator. Trigger: next ADR-0017 adjacent work, OR as a standalone test-correctness commit. Owner-step: step 8.

- **Step-8 `ops_admin_rpcs_test.ts` — one test red in isolation (observed passing at step-8 review close-out; cause unattributed, flake suspected).** `stir_ops_list_voice_sessions: aggregates by trace_id + sorts by token use` was failing when running `ops_admin_rpcs_test.ts` alone (21/22 in 430ms) at the step-6 review sweep. Passes both in isolation and in the full suite at commit `cde2bd5` (step-8 review bundle close-out). No bundle commit touched the test file (`git log --follow` confirms last modification was step-8 phase 2 `80c2ccc`, pre-bundle) or the `stir_ops_list_voice_sessions` RPC body. Root cause unattributed — flake in the pre-bundle isolation conditions suspected; repeated `supabase db reset` cycles through bundle verification may have produced a cleaner baseline DB state. Entry retained (not deleted) so future re-recurrence is diagnosed against known history. Owner-step: revisit if recurrence observed.

- **Pre-work test-file smoke rule** (SCA-156). Before adding a new test to an existing test file, run the existing tests in that file first. Surfaces any pre-existing red tests before new work compounds the diagnostic load. Caught in the P1-N session: 4 pre-existing `realtime_session_test.ts` tests had been red due to `all_steps` schema drift; in the P1-O session: 7 pre-existing `voice_turn_usage_test.ts` tests had been red due to missing Premium promote + session_closed assertion drift. Adding a new test in either case before running existing tests would have masked a subset of findings. Applies to every agent every time they touch a test file. Owner-step: any agent, any commit.

- **`savedMealEntries` predicate's `isFavorite == YES` clause is redundant post-SCA-10** (2026-05-05). After the SCA-10 fix, `setFavorite` writes `isSaved = true` on every call, so any plan with `isFavorite == YES` also has `isSaved == YES` going forward. The `isFavorite == YES` clause survives only to cover pre-fix rows whose users haven't tapped the star post-fix. Once the install base has had ≥1 release-cycle of post-fix usage, the clause can be dropped — the predicate simplifies to `isSaved == YES OR <SUBQUERY cooked-once>`. Cooked-once SUBQUERY also stays as a separate path (sessions can land via TestFlight tooling without `markCompleted`'s `isSaved` write firing on older code paths). Trigger: 2 weeks post-public-launch OR next savedMealEntries refactor. Owner-step: as triggered.

- ~~**`SavedMealsView` doesn't observe Core Data / CloudKit changes**~~ — **resolved 2026-05-08 by SCA-152 (Option b + remote-change extension)**. `SavedMealsView` now listens to two `NotificationCenter` publishers: `.NSManagedObjectContextDidSave` (in-process saves — Cook Mode finishing, favorite toggle elsewhere, leftovers solve persistence) AND `.NSPersistentStoreRemoteChange` (CloudKit-pushed remote changes that auto-merge into viewContext but don't fire didSave). Both trigger a single 300ms-debounced `load()` so a flurry of changes coalesce to one repository fetch. Manual cross-device test pending — code path is the standard CloudKit + NSPersistentCloudKitContainer pattern with `automaticallyMergesChangesFromParent` already enabled, so the wiring is cookbook-correct; ticket's 5s-window AC is comfortable inside the 300ms debounce + push latency budget. The original ticket suggested only `.NSManagedObjectContextDidSave`; remote-change notification was added because the auto-merge that lands CloudKit pushes does NOT fire didSave on the receiving viewContext (it fires `NSManagedObjectContextObjectsDidChange`), so the ticket's cross-device AC requires the broader listener.

- **Shared-schema fixture compile-time enforcement** (SCA-147 path-b follow-up; Sprint-B-review S2). Path (a) shipped — `tests/_helpers/fixtures/realtime_recipe_context.ts` factory consumed by `realtime_session_test.ts` + `cook_turn_test.ts`, with `_shared/validation.ts:348-356` comment block calling out the lockstep-update rule. Path (b) — typing the factory's return as `z.input<typeof RealtimeRecipeContext>` so a future schema tighten produces a `deno check` failure rather than a runtime VAL-01 — was deferred. Path (a) catches drift via review discipline + runtime; path (b) catches it at compile time. Path (b) is one-line if the schema is exported (which it currently is not — it's a module-private const). Trigger: next shared-schema tighten that's awkward to track in review, OR a future agent shipping a fixture-only PR that fails CI on a schema mismatch they didn't notice. Owner-step: as triggered.

- ~~**Three backend handlers emit zero PostHog — observability gap**~~ — **resolved 2026-05-07 by SCA-62**. `session-bootstrap` now emits `app_users_bootstrapped` + `app_users_merged`; `revenuecat-webhook` emits `entitlement_state_changed` + new BILLING_ISSUE event for SCA-77; `ops-flag-output` emits `ops_admin.flagged_outputs.created`. Original entry retained in "Recently resolved". (Original 2026-04-24 telemetry audit G8 context follows.) `session-bootstrap/index.ts`, `revenuecat-webhook/index.ts`, `ops-flag-output/index.ts` perform meaningful state transitions (new-user creation + alias-forward; entitlement-state changes; user-flagged-output submission) but emit zero PostHog events. The user-side iOS `entitlement_state_changed` is the only signal for billing transitions and runs on next foreground (delayed). Brief's "no new event categories beyond spec §9" rule kept these out of the telemetry-wiring bundle. Trigger: **before the unified observability dashboard ships, these three handlers must emit** — the dashboard's billing / identity / flag-rate tiles depend on them. Owner-step: pre-dashboard-launch.

- **Backend test environment consolidation** (SCA-141). Two related findings cost ~20 messages of debugging in the P1-N session: (1) `cp Backend/supabase/.env Backend/supabase/functions/.env` required before `supabase start` or edge runtime crashes on `STIR_JWT_SECRET missing` (edge runtime loads secrets independently of the Deno test runner's env), (2) stray `supabase/` directory at repo root shadows `Backend/supabase/` for some tools. Consolidate via: delete the repo-root `supabase/`, document the env-file split in `Backend/supabase/README.md`, OR use `config.toml` `[edge_runtime.secrets]` block to reference a single env source. Trigger: next new-developer setup, OR next time an agent hits the same debugging loop. Owner-step: step 8 (ops hardening).

- **`SpeechFallbackService` LOC growth — observation only** (SCA-155). P1-Q harness (commit `05787c3`) added ~82 LOC (759 → 841). No prior watch threshold was set for this file; 841 LOC is noted for trend-tracking, not as a hit against a predefined threshold (RealtimeSession's 3200 / CookModeViewModel's 2000 remain the only calibrated triggers). If the file crosses the 2000-LOC threshold (matching CookModeViewModel's trigger shape), revisit extraction patterns analogous to the `RealtimeSession` three-part split. Trigger: file crosses 2000 LOC, OR a refactor-worthy cluster emerges. Owner-step: as triggered.

- **`cost_anomaly_sentry_dispatch_test.ts` flake — `dispatch: valid DSN + unsent rows` failing in isolation, passing on retry** (SCA-154; telemetry bundle Phase E observation, 2026-04-24). First run of the test file failed at `assertNotEquals(after?.dispatched_at, null)` (line 64); immediate re-run passed 3/3. Suspected cause: residual cost_anomaly rows from prior test runs filling the dispatch function's LIMIT-50 window before the test row got picked up. Same shape as the `stir_ops_list_voice_sessions` flake from step-8 review (orthogonal to bundle scope, retained for re-recurrence diagnosis rather than fixed). Owner-step: revisit if recurrence is observed (frequency, environmental correlation).

---

## Recently resolved (purge after grace period)

- **Tonight `Solve again` tile** — resolved 2026-04-28. Wired in `TonightHomeView.handleSolveAgainTap` → `RootCoordinator.requestSolveAgain(...)` → `SolveAgainRoot` cover. The new `SolveAgainRoot` (private struct in `TonightHomeView.swift`) hosts a `NavigationStack` with the `ConstraintsSheet → DinnerOptionsView → DishPreviewView` flow seeded from `SolveRepository.latestPantryIngredients(for:)`. Reuses `ScanFlowRoot.Route` as the path-element type.

- **Tonight `Other options` tile** — resolved 2026-05-05. Wired in `TonightHomeView.presentOtherOptions` → `RootCoordinator.requestOtherOptions(...)` → `OtherOptionsRoot` cover. New `OtherOptionsRoot` (`Stir/Features/Tonight/OtherOptionsRoot.swift`) hosts a `NavigationStack` listing the OTHER 1–2 dishes from the same `MealSolveRequest` as the current Tonight pick. `SolveRepository.latestOtherOptions(excluding:for:)` returns value-type `OtherOption` projections; `SolveRepository.rehydrateDishCard(forSuggestedDishId:)` reconstructs wire-shape `DishCard` values from persisted Core Data so `DishPreviewView` can be reused as the destination (no new preview UI). `SolveViewModel.seedFromPersistedSolve(inputs:)` primes slots + persistedSuggestedDishIDs (rank-aligned, size-3 padded) without touching the AI dispatch stream. Cover dismissal mirrors `SolveAgainRoot` via `coordinator.activeCookLaunch` observation. Telemetry intentionally NOT added to avoid spec §15 + CLAUDE.md churn — captured below as a separate deferred item.

- **Backend retention cron for `ai_request_log`** — resolved 2026-05-07 (SCA-63). Migration `20260508000001_ai_request_log_retention_cron.sql` schedules `stir-cleanup-ai-request-log` at hourly :23, deleting rows with `created_at < now() - interval '30 days'` in 1000-row batches. Covers both general AI request logs and voice turn metadata (rows with `feature_key='cook_mode_realtime'`). Original ticket also listed `voice_turn_usage`, `substitution_events`, `outcome_feedback` — discovery confirmed `voice_turn_usage` is just an `ai_request_log` filter (no separate table) and the latter two are CloudKit-only per the data-ownership boundary; Privacy Policy §6 retention table corrected in the same commit (substitution events + outcome feedback moved from "Stir backend" to the CloudKit section). Prod deploy via `supabase db push` left for a focused session.

- **Sentry tag `user_hash` → `canonical_user_key_hash` rotation** — code-side resolved 2026-05-07 (SCA-59). Added a "Sentry tag deprecation timeline" subsection to `docs/telemetry/canonical-properties.md` §9.1 documenting the 2026-04-24 → 2026-05-08 transition window; amended ADR 0027 §Consequences to record the cutover and unblock dashboard rotation. Sentry workspace dashboard / saved-search / alert-rule sweep is a manual UI-only step left to the workspace owner (see SCA-59 follow-up comment) — pre-cutover events under `tags.user_hash` will age out of Sentry retention naturally; new dashboards should reference only `tags.canonical_user_key_hash`. iOS-side `canonical_key_hash` rename remains an open deprecation per ADR 0027 §9 (waits for next organic touch of `RootCoordinator.swift` / `SupabaseSessionClient.swift` / `CookModeViewModel.swift`).

- **In-app CCPA deletion flow — primary surface** — resolved 2026-05-07 (SCA-61). Five components shipped end-to-end: (a) iOS `Stir/Features/Settings/DeleteMyDataView.swift` Settings → "Delete my data" with type-to-confirm gate, plain-language inventory, mailto fallback for users who can't authenticate; (b) `Backend/supabase/functions/users-delete-request/index.ts` POST endpoint w/ JWT auth, Zod-validated empty body, idempotent insert keyed on the unique partial index `(canonical_user_key) WHERE state IN ('pending','approved','processing')`, hourly per-IP rate cap of 5; (c) migration `20260508000002_init_deletion_requests.sql` with state ENUM + `external_refs_json` checkpoint slot + service-role-only RLS; (d) ops admin tab `ops/src/pages/DeletionRequestsPage.tsx` with state-filtered list + Approve mutation that flips `pending → approved` + audit_log row + ops PostHog event; (e) AdminAction registry extended w/ `deletion_requests.list` + `deletion_requests.approve`. Privacy Policy §7.7 rewritten to declare in-app as primary, email as fallback. Prod deploy: migration applied via `supabase db push`, both `users-delete-request` + `ops-admin` Edge Functions deployed to `ktqajarcomzplnpbczfo`. Fulfillment worker (cross-system erase + `approved → completed | failed` transitions) deferred to SCA-88.

- **Trial reminder feature — full retirement** — resolved 2026-05-07 (SCA-74). Deleted `Stir/Integrations/Notifications/TrialReminderScheduler.swift` + `StirTests/Unit/TrialReminderSchedulerTests.swift`; stripped `Preferences.trialReminder` and `setTrialReminder(_:)` from `NotificationPreferencesStore`; removed the `trialReminders` DI param + property + `updateTrialReminder()` method from `RootCoordinator` (5 call sites); removed the `trial_reminder_sent` builder from `BillingTelemetryProperties` and the `trialReminderSent` enum case from `PostHogClient`; pulled the wire field from `PushRegisterRequest.NotificationPrefs` (iOS DTO) and from the backend `validation.ts` Zod schema, `push-register/index.ts` derivation, `pgmq-dispatch/index.ts` template enum, and `_shared/apns.ts` `APNsCategory`. Spec §8 row 949 + §15 `trial_reminder_sent` row removed; CLAUDE.md telemetry list + pre-launch-chrome-polish entry updated; pbxproj entries dropped. iOS now relies on Apple's system trial-end reminder. Migration `20260419000018_device_installations_push_register.sql` column comment intentionally left as historical (immutable migration rule).

- **Spec §6 sync — `METHOD-NOT-ALLOWED-01` + `VAL-01` field_errors shape + `ENT-*` paywall trigger mapping** — resolved 2026-05-07 (SCA-78). Spec §6 now (a) carries the `METHOD-NOT-ALLOWED-01` row + explanation paragraph next to AUTH-01/VOICE-SESSION-01, (b) inlines the `VAL-01` `field_errors` JSON example so §6 reads standalone, and (c) maps each `ENT-*` code to its `paywall_viewed.trigger` value with origin-surface notes. Predates step-6 remediation; resolved during the post-SCA-58 audit execution.

- **Telemetry for Other Options funnel** — deferred 2026-05-05. The just-resolved `OtherOptionsRoot` flow ships without dedicated PostHog events because adding new event names requires updating spec §15 + CLAUDE.md per the project's own rule. Existing `suggested_dish_selected` fires on alt tap (`rank` reflects 2 or 3, accurate). To add when the spec-update PR lands: `other_options_viewed { available_count: 0|1|2 }` on cover present + a `source: "tonight_other_options" | "solve_options"` property on `suggested_dish_selected`. Trigger: next spec §15 amendment touch, OR first measurement need. Owner-step: as triggered.

- **`RealtimeSession.swift` 3-part split** — resolved 2026-05-07 / 2026-05-08 (SCA-79). Shipped as a 4-file Swift-extension reorganization (commits `441563e` AudioIO bucket → `0b0bced` Transport bucket → `646d0a3` StateMachine bucket → `105ef55` SCA-161 handleTransportError relocation). Final shape: `RealtimeSession.swift` (1,427 LOC: public type, init, public API, test hooks, 30+ stored props, top-level enums/structs), `RealtimeSessionAudioIO.swift` (~280 LOC: mic forward + audio interruption + foreground mic-permission), `RealtimeSessionTransport.swift` (~648 LOC: mint, dispatcher, awaitSetupComplete, tool-call round-trip), `RealtimeSessionStateMachine.swift` (~1,420 LOC: refreshSession, watchdog, pre-mint, finalizeTurn, frame routing, recordTurnAsTransportError, handleTransportError). Verified bug-for-bug identical to baseline `9a9de47` via 10-agent /review-orchestrator pass + 38/38 RealtimeSession unit tests green. **Note**: this is a FILE split, not a TYPE extraction — the original charter's RealtimeTransport / RealtimeAudioPipeline / RealtimeSessionOrchestrator + LiveSessionRefreshCoordinator class shape was deferred to a follow-up tracked above as "RealtimeSession proper-type extraction (post-SCA-79 follow-up)" with explicit triggers. Visibility relaxations + comment guards filed as SCA-159; doc-comment fidelity restoration filed as SCA-160. Both prior deferred-work entries on this topic (lines 73 and 101 in the pre-SCA-163 version of this doc) are subsumed by this resolution.

- **CookModeViewModel voice-telemetry extraction** — resolved 2026-05-08 (SCA-80, branch `refactor/sca-80-voice-telemetry`). New `Stir/Features/CookMode/VoiceSessionTelemetry.swift` (@MainActor `final class`) owns the per-Live-session `$ai_trace` accumulator state (`liveTurnSummaries` / `voiceSessionStartedAt` / `voiceSessionTraceID` / `voiceSessionPathLabel` / `voiceSessionInputState`) plus the canonical bodies of `seedTrace`, `fireCloseTrace`, `recordLiveTurnSummary`, `recordVoiceSessionRefresh`, `recordVoiceTurnStuckWatchdogFired`, `emitVoiceSubstitutionRequested/Resolved`, `emitCookTurnResolved`, `emitScreenError`, `emitVoiceAffordance`. New `VoiceAffordanceResult` enum locks the wire vocabulary on `voice_affordance_tapped.result`. CookModeViewModel keeps thin forwarders so `CookModeRoot.wireVoiceDriver` + the CookMode test suite (47 cases incl. new late-frame regression test) compile + pass without changes. `recordTurnTranscript` stays on the VM (UI-state for transcript card, not telemetry). `currentTurnBargedIn` stays on the VM; helper `recordLiveTurnSummary` takes a `consumeBargeInFlag: () -> Bool` closure invoked PAST the trace-id guard so a late `turnComplete` post-`fireCloseTrace` doesn't silently consume the flag (review W1). PostHog wire shape preserved bit-identical: zero new event names, zero new property values, identical fire conditions. Two commits: `92cf859` (initial extract) + `59bd1d5` (multi-agent review fixes — W1-W5 + S1-S8).
