# Stir

Weeknight dinner copilot for iPhone — scan, solve, cook. See `CLAUDE.md` for the working orientation pack and `Specs/Stir-Full-Spec.md` for the authoritative product spec.

Step-1 build (this commit set) delivers the Supabase operational backend only. iOS scaffold is step 2. Full build order lives in `CLAUDE.md` § "Build order".

> Detailed setup, verification, deferred-work, and assumptions sections land in commit 5 once the backend is complete.
